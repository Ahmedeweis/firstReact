# ✅ تم إصلاح محاذاة RTL في تفاصيل الطلب

## المشكلة
في صفحة تفاصيل الطلب، كان هناك مشكلتان في الوضع العربي (RTL):
1. **الإجماليات (Totals):** كانت تظهر في اليسار (وهو الوضع الافتراضي للغة الإنجليزية Right-aligned).
2. **سجل الطلب (History):** كان الملخص يظهر في اليسار والخط الزمني في اليمين.

## الحلول المطبقة

### 1. إصلاح الإجماليات (`order-details-item.tsx`)
تم تعديل `Stack` الخاص بالإجماليات ليأخذ `alignItems: 'flex-start'` في وضع RTL، مما يجعله يظهر في **اليمين**.

```tsx
<Stack
  // ...
  sx={{
    // ...
    '[dir="rtl"] &': { alignItems: 'flex-start', textAlign: 'left' },
  }}
>
```

### 2. إصلاح السجل (`order-details-history.tsx`)
تم عكس اتجاه الـ Flexbox في وضع RTL باستخدام `row-reverse`. هذا يجعل الملخص يظهر في **اليمين** (بداية السطر في LTR) والخط الزمني في اليسار، وهذا هو التخطيط المألوف حيث يكون الشريط الجانبي (Side Panel) في الجهة الموافقة لاتجاه القراءة.

```tsx
<Stack
  // ...
  sx={{
    // ...
    '[dir="rtl"] &': {
      flexDirection: { md: 'row-reverse' },
    },
  }}
>
```

## النتيجة
✅ الإجماليات الآن تظهر في **اليمين** في العربية.
✅ ملخص السجل (التواريخ) يظهر في **اليمين** بجوار الخط الزمني في العربية.
✅ التنسيق الإنجليزي (LTR) لم يتأثر.

تم الإصلاح! 🚀
